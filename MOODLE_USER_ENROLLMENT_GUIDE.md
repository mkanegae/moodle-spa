# Moodleユーザー登録・コース割当 ガイド

## 1. ユーザー一括登録CSV

### ファイル: `moodle-user-upload-template.csv`

### 必須フィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **username** | ユーザー名（ログインID、一意） | `yamada.taro` |
| **firstname** | 名 | `太郎` |
| **lastname** | 姓 | `山田` |
| **email** | メールアドレス（一意） | `yamada.taro@example.com` |

### 推奨フィールド

| フィールド名 | 説明 | 例 | デフォルト値 |
|------------|------|-----|-------------|
| **password** | パスワード | `Password123!` | ランダム生成 |
| **auth** | 認証方法 | `manual`, `ldap`, `oauth2` | `manual` |
| **city** | 都市 | `東京` | - |
| **country** | 国コード（2文字） | `JP` | サイトデフォルト |
| **lang** | 言語 | `ja`, `en` | サイトデフォルト |
| **idnumber** | ID番号 | `USR001` | - |
| **institution** | 所属組織 | `株式会社サンプル` | - |
| **department** | 部署 | `開発部` | - |
| **phone1** | 電話番号 | `03-1234-5678` | - |

### カスタムプロフィールフィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **profile_field_XXX** | カスタムフィールド | `profile_field_employeeid` |

カスタムフィールドは `profile_field_` プレフィックスを付けて使用します。

### 使用手順

1. **Moodle管理画面にログイン**
2. **サイト管理 > ユーザー > アカウント > ユーザーをアップロード** に移動
3. CSVファイルを選択
4. アップロードオプションを設定：
   - **既存のユーザーアカウントの場合**: 更新 / スキップ / 追加を選択
   - **既存でない場合**: 新規ユーザーを作成
   - **パスワードなし**: ランダムパスワード生成 / スキップ
5. プレビューを確認
6. 「ユーザーをアップロード」をクリック

### パスワードのルール

Moodleのパスワードポリシーに従う必要があります（デフォルト）：
- 最低8文字
- 英大文字1文字以上
- 英小文字1文字以上
- 数字1文字以上
- 記号1文字以上（`!@#$%^&*`など）

例: `Password123!`

### 認証方法（auth）

| 値 | 説明 |
|---|------|
| `manual` | 手動アカウント（標準） |
| `ldap` | LDAP認証 |
| `oauth2` | OAuth2認証（Google, Microsoftなど） |
| `email` | メールベース自己登録 |
| `shibboleth` | Shibboleth認証 |

### 国コード（ISO 3166-1 alpha-2）

| コード | 国 |
|-------|-----|
| `JP` | 日本 |
| `US` | アメリカ |
| `GB` | イギリス |
| `CN` | 中国 |
| `KR` | 韓国 |

---

## 2. コース割当（エンロールメント）CSV

### ファイル: `moodle-course-enrollment-template.csv`

### 必須フィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **username** | ユーザー名 | `yamada.taro` |
| **course** | コース短縮名 | `MATH101` |
| **role** | ロール | `student`, `editingteacher`, `teacher` |

### オプションフィールド

| フィールド名 | 説明 | 例 | デフォルト値 |
|------------|------|-----|-------------|
| **group** | グループ名 | `グループA` | なし |
| **enrolstatus** | 登録ステータス | `0` (有効), `1` (停止) | `0` |

### ロールの種類

| ロール名 | 説明 | 権限 |
|---------|------|------|
| **student** | 学生 | コンテンツ閲覧、課題提出 |
| **teacher** | 教師（非編集） | 閲覧、評価、レポート |
| **editingteacher** | 教師（編集） | フルアクセス、コンテンツ編集 |
| **manager** | マネージャー | サイト全体の管理 |
| **coursecreator** | コース作成者 | 新規コース作成 |

### 使用手順

1. **Moodle管理画面にログイン**
2. **サイト管理 > コース > コース登録をアップロード** に移動
3. CSVファイルを選択
4. オプションを設定：
   - **既存の登録の場合**: 更新 / スキップ
   - **登録方法**: 手動登録
5. プレビューを確認
6. 「登録をアップロード」をクリック

### グループ機能

グループを指定すると、コース内で学生をグループ分けできます：
- グループディスカッション
- グループ課題
- グループ別のアクティビティ表示

グループ名が存在しない場合、自動的に作成されます。

---

## 3. 統合ワークフロー例

### ステップ1: ユーザー登録
1. `moodle-user-upload-template.csv` を編集
2. Moodleにユーザーを一括登録

### ステップ2: コース作成
1. `moodle-course-upload-template.csv` を編集
2. Moodleにコースを一括作成

### ステップ3: コース割当
1. `moodle-course-enrollment-template.csv` を編集
2. ユーザーをコースに一括割当

---

## 4. サンプルデータの説明

### ユーザーサンプル

| ユーザー名 | 氏名 | 部署 | 社員番号 |
|-----------|------|------|---------|
| yamada.taro | 山田 太郎 | 開発部 | EMP001 |
| tanaka.hanako | 田中 花子 | 営業部 | EMP002 |
| suzuki.ichiro | 鈴木 一郎 | 人事部 | EMP003 |
| sato.yuki | 佐藤 由紀 | 開発部 | EMP004 |
| watanabe.kenji | 渡辺 健二 | マーケティング部 | EMP005 |

### コース割当サンプル

- **山田 太郎**: MATH101（教師）, PROG301（学生）
- **田中 花子**: ENG201, MGT401（グループA）
- **鈴木 一郎**: MATH101（グループB）, DATA501
- **佐藤 由紀**: PROG301, DATA501
- **渡辺 健二**: ENG201, MGT401（グループA）

---

## 5. トラブルシューティング

### エラー: "ユーザー名が既に存在します"
- `username` は一意である必要があります
- 既存ユーザーを更新する場合は、アップロード時に「更新」を選択

### エラー: "メールアドレスが既に存在します"
- `email` も一意である必要があります
- 既存のメールアドレスを確認してください

### エラー: "パスワードポリシーに違反しています"
- Moodleのパスワードポリシーを確認
- **サイト管理 > セキュリティ > サイトポリシー** で設定を確認

### エラー: "コースが見つかりません"
- コース割当CSVの `course` フィールドは、コースの `shortname` を使用
- コースが事前に作成されているか確認

### エラー: "ユーザーが見つかりません"
- コース割当前に、ユーザーが登録されているか確認
- `username` が正確か確認

### 文字化けする
- CSVファイルのエンコーディングをUTF-8（BOM付き）で保存
- ExcelではなくVSCodeやGoogle スプレッドシートを推奨

---

## 6. CSVエンコーディング

### 推奨エンコーディング
- **UTF-8 with BOM** (UTF-8 BOM付き)

### VSCodeで保存
1. ファイルを開く
2. 右下の「UTF-8」をクリック
3. 「エンコード付きで保存」を選択
4. 「UTF-8 with BOM」を選択

### Google スプレッドシート
- 「ファイル > ダウンロード > カンマ区切り形式(.csv)」で自動的にUTF-8

---

## 7. セキュリティのベストプラクティス

### パスワード管理
- CSVファイルに平文パスワードを保存しないように注意
- アップロード後は即座にCSVファイルを削除
- 可能であれば、初回ログイン時にパスワード変更を強制

### パスワード自動生成
アップロード時に `password` フィールドを空にすると、Moodleがランダムパスワードを生成し、ユーザーにメール送信します。

### LDAP/OAuth2連携
大規模組織では、手動パスワード管理ではなく、LDAP/OAuth2認証を推奨します。

---

## 8. 一括削除・更新

### ユーザーの一括更新
同じCSVファイルで、アップロード時に「既存のユーザーアカウントの場合: 更新」を選択すると、既存ユーザー情報を更新できます。

### ユーザーの一括削除
CSVに `deleted` フィールドを追加：
```csv
username,deleted
yamada.taro,1
```

### コース登録解除
CSVに `enrolstatus` を `1` に設定：
```csv
username,course,role,enrolstatus
yamada.taro,MATH101,student,1
```

---

## 9. カスタムプロフィールフィールドの作成

### 手順
1. **サイト管理 > ユーザー > ユーザープロフィールフィールド** に移動
2. 新しいフィールドを作成（例: `employeeid`）
3. CSVで `profile_field_employeeid` として使用

### よく使われるカスタムフィールド
- `profile_field_employeeid` - 社員番号
- `profile_field_jobtitle` - 役職
- `profile_field_hiredate` - 入社日
- `profile_field_manager` - 上司名

---

## まとめ

1. **ユーザー登録**: `moodle-user-upload-template.csv`
2. **コース作成**: `moodle-course-upload-template.csv`
3. **コース割当**: `moodle-course-enrollment-template.csv`

この順序で実行することで、Moodleの初期セットアップを効率的に行えます。
