# Moodleコース一括登録CSV ガイド

## ⚠️ 重要: カテゴリを先に作成してください

コースをアップロードする前に、**必ずカテゴリを作成してください**。

1. **カテゴリCSVガイド**: `MOODLE_CATEGORY_UPLOAD_GUIDE.md` を参照
2. **カテゴリテンプレート**: `moodle-category-upload-template.csv` を使用
3. カテゴリ作成後、カテゴリIDを確認してからコースCSVを作成

### エラー: "カテゴリIDでカテゴリを解決できませんでした"

このエラーが出る場合、指定したカテゴリIDがMoodleに存在しません。
先にカテゴリを作成し、正しいIDを確認してください。

## CSVフォーマット説明

### 必須フィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **shortname** | コースの短縮名（一意である必要がある） | `MATH101` |
| **fullname** | コースのフルネーム | `数学基礎コース` |
| **category** | カテゴリID（数値） | `1` |

### 推奨フィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **summary** | コースの説明文 | `数学の基礎を学ぶコース` |
| **format** | コースフォーマット | `topics`, `weeks`, `social`, `singleactivity` |
| **visible** | 表示/非表示 | `1` (表示), `0` (非表示) |
| **startdate** | 開始日（UNIXタイムスタンプ） | `1704067200` (2024-01-01) |
| **enddate** | 終了日（UNIXタイムスタンプ） | `1719792000` (2024-07-01) |
| **tags** | タグ（カンマ区切り） | `数学,基礎,必修` |

### その他の利用可能なフィールド

| フィールド名 | 説明 | 例 |
|------------|------|-----|
| **idnumber** | コースID番号 | `COURSE-2024-001` |
| **numsections** | セクション数 | `10` |
| **newsitems** | ニュース項目数 | `5` |
| **showgrades** | 成績表示 | `1` (表示), `0` (非表示) |
| **showreports** | レポート表示 | `1` (表示), `0` (非表示) |
| **maxbytes** | 最大アップロードサイズ | `2097152` (2MB) |
| **groupmode** | グループモード | `0` (なし), `1` (分離), `2` (可視) |
| **groupmodeforce** | グループモード強制 | `1` (強制), `0` (任意) |
| **lang** | 言語 | `ja` (日本語), `en` (英語) |

## 使用手順

### 1. カテゴリの作成とIDの確認

**ステップ1: カテゴリを作成**

まず `MOODLE_CATEGORY_UPLOAD_GUIDE.md` を参照してカテゴリを作成します。

**ステップ2: カテゴリIDを確認**

Moodle管理画面でカテゴリIDを確認します：

1. Moodle管理画面にログイン
2. **サイト管理 > コース > コースとカテゴリの管理** に移動
3. カテゴリをクリックし、URLの `categoryid=` パラメータで確認
   - 例: `https://your-moodle.com/course/management.php?categoryid=5`

**ステップ3: コースCSVを作成**

確認したカテゴリIDを使用してコースCSVの `category` フィールドを設定します。

### 2. タグの設定

タグは以下の形式で設定できます：

- **カンマ区切り**: `"数学,基礎,必修"`
- **複数のタグ**: ダブルクォートで囲む
- **日本語タグ**: そのまま使用可能

### 3. 日付の設定（UNIXタイムスタンプ）

UNIXタイムスタンプへの変換方法：

#### オンラインツールを使用
- https://www.unixtimestamp.com/

#### Excelで変換
```excel
=(日付セル - DATE(1970,1,1)) * 86400
```

#### Pythonで変換
```python
from datetime import datetime
timestamp = int(datetime(2024, 1, 1).timestamp())
print(timestamp)  # 1704067200
```

### 4. CSVファイルのアップロード

1. Moodle管理画面にログイン
2. **サイト管理 > コース > コースをアップロード** に移動
3. CSVファイルを選択してアップロード
4. プレビューを確認
5. 「コースをアップロード」をクリック

## CSVファイルのエンコーディング

- **推奨**: UTF-8（BOM付き）
- 日本語を含む場合は必ずUTF-8で保存してください

### Excelで保存する場合の注意

Excelで保存すると文字化けする可能性があります。以下の方法を推奨：

1. **メモ帳/VSCodeで保存**: UTF-8エンコーディングを選択
2. **Google スプレッドシート**: CSVダウンロード時に自動的にUTF-8
3. **LibreOffice Calc**: 保存時に文字コードを選択可能

## サンプルデータの説明

`moodle-course-upload-template.csv` には以下のサンプルコースが含まれています：

1. **MATH101** - 数学基礎（カテゴリ1, タグ: 数学/基礎/必修）
2. **ENG201** - ビジネス英語（カテゴリ2, タグ: 英語/ビジネス/コミュニケーション）
3. **PROG301** - プログラミング入門（カテゴリ1, タグ: プログラミング/Python/IT/初心者）
4. **MGT401** - プロジェクト管理（カテゴリ2, タグ: マネジメント/アジャイル/ビジネス）
5. **DATA501** - データ分析基礎（カテゴリ1, タグ: データ分析/Excel/ビジネスインテリジェンス）

**⚠️ 注意**: サンプルCSVではカテゴリID `1` と `2` を使用していますが、これらのカテゴリがMoodleに存在しない場合、エラーが発生します。

**使用前に必ず**:
1. `moodle-category-upload-template.csv` でカテゴリを作成
2. 作成されたカテゴリIDを確認
3. コースCSVの `category` フィールドを実際のIDに更新

## トラブルシューティング

### エラー: "カテゴリIDでカテゴリを解決できませんでした" / "カテゴリが見つかりません"

**原因**: 指定したカテゴリIDがMoodleに存在しない

**解決方法**:
1. 先に `MOODLE_CATEGORY_UPLOAD_GUIDE.md` を参照してカテゴリを作成
2. Moodle管理画面で実際のカテゴリIDを確認
3. コースCSVの `category` フィールドを正しいIDに更新
4. **サイト管理 > コース > コースとカテゴリの管理** で既存カテゴリを確認

### エラー: "コースの短縮名が既に使用されています"
- `shortname` は一意である必要があります
- 既存のコースと重複しないように変更してください

### 文字化けする
- CSVファイルのエンコーディングをUTF-8（BOM付き）で保存してください

### タグが登録されない
- タグフィールドをダブルクォートで囲んでいるか確認してください
- Moodleのバージョンによってはタグ機能の有効化が必要です

## カテゴリ構造の例

実際の運用では、以下のようなカテゴリ構造を推奨します：

```
1. 必修科目
   - 1-1. 基礎教養
   - 1-2. 専門基礎
2. 選択科目
   - 2-1. ビジネス系
   - 2-2. IT系
   - 2-3. 語学系
3. 特別講座
```

各カテゴリIDを確認し、CSVの `category` フィールドに適切なIDを設定してください。
