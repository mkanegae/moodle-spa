# カテゴリアップロード クイックスタートガイド

## 📋 準備するもの

- [ ] Moodle管理者アカウント
- [ ] `moodle-category-upload-template.csv` ファイル
- [ ] メモ帳（カテゴリIDを記録用）

---

## 🚀 5分でできる！カテゴリアップロード

### ステップ1: Moodleにログイン (30秒)

```
https://your-moodle.com
↓
管理者アカウントでログイン
↓
「サイト管理」をクリック
```

---

### ステップ2: カテゴリアップロード画面を開く (30秒)

**方法1: メニューから**
```
サイト管理
  → コース
    → カテゴリをアップロード
```

**方法2: 検索から**
```
サイト管理画面の検索ボックス
  → 「カテゴリ」と入力
    → 「カテゴリをアップロード」を選択
```

**方法3: 直接URL**
```
https://your-moodle.com/course/uploadcategories.php
```

---

### ステップ3: CSVファイルをアップロード (1分)

1. **ファイルを選択**
   - 「ファイルを選択」ボタンをクリック
   - `moodle-category-upload-template.csv` を選択

2. **設定を確認**
   - エンコーディング: **UTF-8** ✅
   - 区切り文字: **カンマ (,)** ✅
   - アップロードモード: **新規カテゴリのみ作成** ✅

3. **プレビュー**
   - 「プレビュー」ボタンをクリック

---

### ステップ4: 内容を確認してアップロード (1分)

1. **プレビュー画面で確認**
   ```
   ✅ カテゴリ名が正しい
   ✅ 親カテゴリが正しい（トップレベルは「-」と表示）
   ✅ 文字化けしていない
   ```

2. **アップロード実行**
   - 「カテゴリをアップロード」ボタンをクリック
   - 完了まで待つ（数秒）

3. **結果確認**
   ```
   作成されたカテゴリ: 3 ✅
   エラー: 0 ✅
   ```

---

### ステップ5: カテゴリIDを確認 (2分)

1. **カテゴリ管理画面に移動**
   ```
   サイト管理
     → コース
       → コースとカテゴリの管理
   ```

2. **カテゴリIDをメモ**

   各カテゴリをクリックしてURLを確認：
   ```
   必修科目: categoryid=1  → ID: 1
   選択科目: categoryid=2  → ID: 2
   特別講座: categoryid=8  → ID: 8
   ```

3. **メモ例**
   ```
   カテゴリID一覧:
   ─────────────────
   必修科目: 1
   選択科目: 2
   特別講座: 8
   ```

---

## ✅ 完了！次のステップ

カテゴリIDをメモしたら、次はコースを作成します：

1. `moodle-course-upload-template.csv` を開く
2. `category` 列を確認したIDに更新
3. `MOODLE_CSV_GUIDE.md` の手順に従ってコースをアップロード

---

## 🔧 トラブルシューティング（30秒で解決）

### 問題: 文字化けする

**解決**: CSVをUTF-8で保存し直す

```
メモ帳で開く
  → 「名前を付けて保存」
    → エンコーディング: UTF-8
      → 保存
```

---

### 問題: 「親カテゴリが見つかりません」

**解決**: トップレベルカテゴリを先に作成

```
1. parent=0 の行だけをアップロード
2. カテゴリIDを確認
3. parent=1,2,3 の行をアップロード
```

---

### 問題: 「カテゴリをアップロード」が見つからない

**解決**: 直接URLにアクセス

```
https://your-moodle.com/course/uploadcategories.php
```

---

## 📝 よく使うCSV例

### 例1: シンプルな3カテゴリ

```csv
name,parent
必修科目,0
選択科目,0
特別講座,0
```

### 例2: 階層構造

```csv
name,parent
必修科目,0
  基礎教養,1
  専門基礎,1
選択科目,0
  IT系,4
  語学系,4
```

**注意**: `parent` は実際のカテゴリIDに置き換える

---

## ⏱️ 所要時間の目安

| 作業 | 時間 |
|------|------|
| ログイン + 画面移動 | 1分 |
| CSVアップロード + プレビュー | 1分 |
| アップロード実行 | 30秒 |
| カテゴリID確認 | 2分 |
| **合計** | **約5分** |

---

## 💡 プロのヒント

### ヒント1: 段階的にアップロード

一度に全部ではなく、段階的にアップロードすると安全：

```
1日目: トップレベルカテゴリのみ
2日目: サブカテゴリ
3日目: コース
```

### ヒント2: バックアップを取る

既存のカテゴリがある場合、アップロード前にバックアップ：

```
カテゴリ管理画面
  → 各カテゴリのスクリーンショット
    → 保存
```

### ヒント3: テストデータで練習

本番データの前に、テストカテゴリで練習：

```csv
name,parent
テストカテゴリ1,0
テストカテゴリ2,0
```

アップロード成功 → 削除 → 本番データアップロード

---

## 📞 困ったときは

1. **詳細手順**: `MOODLE_CATEGORY_UPLOAD_STEPS.md` を参照
2. **カテゴリガイド**: `MOODLE_CATEGORY_UPLOAD_GUIDE.md` を参照
3. **コース作成**: `MOODLE_CSV_GUIDE.md` を参照

---

## チェックリスト

カテゴリアップロード完了後、以下を確認：

- [ ] カテゴリが作成された
- [ ] カテゴリ名が正しい（文字化けなし）
- [ ] 階層構造が正しい
- [ ] カテゴリIDをメモした
- [ ] 次のステップ（コース作成）の準備ができた

すべてチェックできたら、コースアップロードに進みましょう！
